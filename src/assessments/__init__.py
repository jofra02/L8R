"""Device Assessment module.

Deterministic, definition-driven assessments over managed devices:
versioned YAML definitions -> deterministic MCP collection -> evidence ->
control evaluation (rules -> parsers -> LLM fallback) -> scoring -> report.
"""
