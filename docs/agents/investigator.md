# Investigator Agent

## Description
The Investigator Agent verifies specific hypotheses generated by the Hypothesis Agent. It selects the highest-priority "proposed" hypothesis, formulates a verification plan, searches for appropriate tools, and executes them to gather targeted evidence.

## Role in Graph
- **Node Name:** `investigator_agent`
- **Upstream:** `supervisor` (routed when a hypothesis needs verification)
- **Downstream:** `enricher_agent` (to process the newly collected evidence)

## Inputs
- `state["hypotheses"]`: List of hypotheses. Targets the highest-ranked one with status `proposed`.
- `state["ticket"]`: Ticket details for context.
- `state["components"]`: List of identified components to scope tool arguments.

## Outputs
- `state["hypotheses"]`: Updates the status of the target hypothesis to `verifying`.
- `state["evidence_refs"]`: Appends new evidence snapshots collected from tool executions.

## Prompts

### 1. Verification Plan
**System:** "You are an expert IT Systems Troubleshooter."
**Goal:** Identify the most effective tool execution to prove/disprove the hypothesis.

### 2. Tool Selection
**System:** "You are an expert Automation Engineer."
**Goal:** Select the specific tool from search results and configure arguments.

## Key Logic & Interactions

- **LLM Model:** Uses `LLM_MODEL_INVESTIGATOR` (e.g., `gpt-5.2`) — maximizing reasoning for hypothesis verification.

### Smart Argument Sanitization (Role-Based)
Expanded executor roles for multi-domain support:
- **EXECUTORS** (device argument): firewall, router, switch, server, host, appliance, controller, gateway, hypervisor, node, cluster, database, storage, loadbalancer.
- **TARGETS** (target/destination argument): subnet, IP, URL, service, process, endpoint, user, application, container, pod, VM, instance.
- **Auto-Correction:** If the LLM incorrectly assigns a target entity to `device`, the agent intercepts and corrects it.

### Tool Search
Uses Qdrant semantic search via `CapabilityRegistry.search_tools(query)` for intent-based tool discovery.

### Adaptive Execution
Tools executed via `AdaptiveExecutor` which handles:
- Self-healing on tool failures (queries Qdrant for documented fixes).
- Missing dependency detection → creates `PendingRequirement` for human input.
- Learned fixes persisted for future use.

### Interaction Flow
1. **Selection:** Picks the top `proposed` hypothesis.
2. **Planning:** Asks LLM *what* to verify (output: search query + reasoning).
3. **Search:** Queries Qdrant tool catalog.
4. **Configuration:** Asks LLM *how* to execute (output: tool name + args).
5. **Execution:** Runs the tool, saves evidence.
6. **State Update:** Marks hypothesis as `verifying`, returns to graph.
